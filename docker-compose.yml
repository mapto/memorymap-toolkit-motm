# For use in development only. Starts two containers - one for MMT, one for postgres
# with volumes for the /app directory and the postgres data. /app dir mapped means that
# changes to the code reloads MMT.

services:
  db:
    image: postgis/postgis:17-3.5-alpine
    volumes:
      - postgres_data:/var/lib/postgresql
      - backups:/backups
    environment:
      - POSTGRES_USER=${DJANGO_DB_USER}
      - POSTGRES_DB=${DJANGO_DB_NAME}
      - POSTGRES_PASSWORD=${DJANGO_DB_PASSWORD}
      - ALLOW_IP_RANGE=0.0.0.0/0
    ports:
      - 5433:5432
    env_file:
      - .env
    platform: linux/amd64
    healthcheck:
      test: ["CMD-SHELL", "pg_isready", "-U${DJANGO_DB_USER}", "-d", "${DJANGO_DB_NAME}"]    
      start_period: 10s

  memorymaptoolkit:
    image: memorymaptoolkit
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - static:/app/static
      - media:/app/media
      - backups:/app/backups
      - .:/app
      - ./memorymap_toolkit/settings/secret_settings_template.py:/app/memorymap_toolkit/settings/secret_settings.py:ro
    depends_on:
      db:
        condition: service_healthy
    command: python manage.py runserver 0.0.0.0:8000 --settings=memorymap_toolkit.settings.local
    ports:
      - 8000:8000
    env_file:
      - .env
    environment:
      - DJANGO_DB_USER=${DJANGO_DB_USER}
      - DJANGO_DB_NAME=${DJANGO_DB_NAME}
      - DJANGO_DB_PASSWORD=${DJANGO_DB_PASSWORD}
      - DJANGO_SECRET_KEY=${DJANGO_SECRET_KEY}
      - DJANGO_ALLOWED_HOSTS=${DJANGO_ALLOWED_HOSTS}
      - DJANGO_SECURE_SSL_HOST=${DJANGO_SECURE_SSL_HOST}
      - DJANGO_SESSION_COOKIE_SECURE=${DJANGO_SESSION_COOKIE_SECURE}
      - DJANGO_CSRF_COOKIE_SECURE=${DJANGO_CSRF_COOKIE_SECURE}
      - DJANGO_DB_HOST=${DJANGO_DB_HOST}
      - DJANGO_SETTINGS_MODULE=memorymap_toolkit.settings.local
      # byHen: aggiunta riga per poter utilizzare la shell nel container
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000')"]
 
volumes:
  postgres_data:
  static:
  media:
  backups: